<%= form_for(@task) do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %>

  <%= f.label :detail %>
  <%= f.text_field :detail %>

  <%= f.submit %>
<% end %>

<%= link_to "Back to Index", tasks_path%>
